'use strict';

/**
 * checkout-information service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::checkout-information.checkout-information');
