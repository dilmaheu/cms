'use strict';

/**
 * recurring-element service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::recurring-element.recurring-element');
