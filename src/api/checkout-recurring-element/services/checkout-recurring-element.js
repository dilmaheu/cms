'use strict';

/**
 * checkout-recurring-element service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::checkout-recurring-element.checkout-recurring-element');
