'use strict';

/**
 * checkout-recurring-element router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::checkout-recurring-element.checkout-recurring-element');
